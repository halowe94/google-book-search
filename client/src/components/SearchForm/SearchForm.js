import React from "react";

function SearchForm({ q, handleInputChange, handleFromSubmit }) {
  return (
    <form>
      <div>
        <label></label>
        <input />
      </div>
      <button></button>
    </form>
  );
}

export default SearchForm;
